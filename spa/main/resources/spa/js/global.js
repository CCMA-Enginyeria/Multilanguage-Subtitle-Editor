/*global
    define, _, require, jitConfig
 */

define(['./utils/spa-devtools'],
    function(SPADebugTools) {
        window.SPADebugTools = SPADebugTools;
        return {
            initialize: function() {
             
            },
        };

    }
);