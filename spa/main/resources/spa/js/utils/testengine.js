/*globals
    define, _, $, AppRouter, console, jQuery, require
*/

define(['./testengine-common'],
    function(TestEngineCommon) {
        /*-
            BUG: al comprimir es obligatorio que exista el resto de JS testengine.js
        -*/
        return _.extend({}, TestEngineCommon, {

        });
    });